diff --git a/Frontend/Components/Layout/MainLayout.razor b/Frontend/Components/Layout/MainLayout.razor
index 5b17a9d..715181b 100644
--- a/Frontend/Components/Layout/MainLayout.razor
+++ b/Frontend/Components/Layout/MainLayout.razor
@@ -1,4 +1,6 @@
-∩╗┐@inherits LayoutComponentBase
+@inherits LayoutComponentBase
+@inject Frontend.Services.AuthService Auth
+@inject IJSRuntime JS
 
 <div class="page">
     <div class="sidebar">
@@ -7,13 +9,22 @@
 
     <main>
         <div class="top-row px-4 d-flex justify-content-end align-items-center gap-2">
-            <NavLink class="btn btn-outline-secondary" href="profile">
-                Profile
-            </NavLink>
+            @if (Auth.IsLoggedIn)
+            {
+                <NavLink class="btn btn-outline-secondary" href="profile">
+                    Profile
+                </NavLink>
 
-            <NavLink class="btn btn-outline-primary" href="login">
-                Login
-            </NavLink>
+                <button class="btn btn-outline-danger" @onclick="HandleLogout">
+                    Logout
+                </button>
+            }
+            else
+            {
+                <NavLink class="btn btn-outline-primary" href="login">
+                    Login
+                </NavLink>
+            }
         </div>
         <article class="content px-4">
             @Body
@@ -27,3 +38,29 @@
     <span class="dismiss">≡ƒùÖ</span>
 </div>
 <ReconnectModal />
+
+@code {
+    protected override async Task OnAfterRenderAsync(bool firstRender)
+    {
+        if (!firstRender || Auth.IsLoggedIn)
+        {
+            return;
+        }
+
+        var storedToken = await JS.InvokeAsync<string?>("localStorage.getItem", "auth_token")
+                           ?? await JS.InvokeAsync<string?>("sessionStorage.getItem", "auth_token");
+        if (!string.IsNullOrEmpty(storedToken))
+        {
+            Auth.TryRestoreToken(storedToken);
+            StateHasChanged();
+        }
+    }
+
+    private async Task HandleLogout()
+    {
+        Auth.Logout();
+        await JS.InvokeVoidAsync("localStorage.removeItem", "auth_token");
+        await JS.InvokeVoidAsync("localStorage.removeItem", "email_hint");
+        await JS.InvokeVoidAsync("sessionStorage.removeItem", "auth_token");
+    }
+}
diff --git a/Frontend/Components/Pages/Profile.razor b/Frontend/Components/Pages/Profile.razor
index 81399b6..c32848b 100644
--- a/Frontend/Components/Pages/Profile.razor
+++ b/Frontend/Components/Pages/Profile.razor
@@ -1,3 +1,292 @@
+@page "/profile"
+@rendermode InteractiveServer
+@inject Frontend.Services.AuthService Auth
+@inject Frontend.Services.ProfileApiService ProfileApi
+@inject NavigationManager Nav
+@inject IJSRuntime JS
+
+<h3>Your profile</h3>
+
+@if (!isLoaded)
+{
+    <p class="text-muted">Loading profileΓÇª</p>
+}
+else if (profile == null)
+{
+    <p class="text-muted">Profile information is not available.</p>
+}
+else
+{
+    <div class="card mb-3 p-3">
+        <h5>@profile.Profile.DisplayName (@profile.Profile.DefaultLanguage)</h5>
+        <p class="mb-1">@profile.Profile.Bio</p>
+        <p class="mb-1">
+            <strong>Genres:</strong>
+            @string.Join(", ", profile.Profile.Genres)
+        </p>
+        <p class="mb-0">
+            @if (!string.IsNullOrWhiteSpace(profile.Profile.SpotifyUrl))
+            {
+                <a href="@profile.Profile.SpotifyUrl" target="_blank" class="me-2">Spotify</a>
+            }
+            @if (!string.IsNullOrWhiteSpace(profile.Profile.YouTubeUrl))
+            {
+                <a href="@profile.Profile.YouTubeUrl" target="_blank">YouTube</a>
+            }
+        </p>
+    </div>
+
+    <ul class="nav nav-pills mb-3">
+        <li class="nav-item">
+            <button class="nav-link @(activeTab == "overview" ? "active" : "")" @onclick="@(() => activeTab = "overview")">
+                Overview
+            </button>
+        </li>
+        <li class="nav-item">
+            <button class="nav-link @(activeTab == "albums" ? "active" : "")" @onclick="@(() => activeTab = "albums")">
+                Albums
+            </button>
+        </li>
+        <li class="nav-item">
+            <button class="nav-link @(activeTab == "tracks" ? "active" : "")" @onclick="@(() => activeTab = "tracks")">
+                Tracks
+            </button>
+        </li>
+        <li class="nav-item">
+            <button class="nav-link @(activeTab == "tours" ? "active" : "")" @onclick="@(() => activeTab = "tours")">
+                Tours
+            </button>
+        </li>
+    </ul>
+
+    @switch (activeTab)
+    {
+        case "albums":
+            <AlbumsSection Albums="profile.Albums" />
+            break;
+        case "tracks":
+            <TracksSection Tracks="profile.Tracks" Albums="profile.Albums" />
+            break;
+        case "tours":
+            <ToursSection Tours="profile.Tours" />
+            break;
+        default:
+            <OverviewSection Albums="profile.Albums" Tracks="profile.Tracks" Tours="profile.Tours" />
+            break;
+    }
+}
+
+@code {
+    private ProfileResponseDto? profile;
+    private bool isLoaded;
+    private string activeTab = "overview";
+
+    protected override async Task OnInitializedAsync()
+    {
+        if (!Auth.IsLoggedIn)
+        {
+            var storedToken = await JS.InvokeAsync<string?>("localStorage.getItem", "auth_token")
+                               ?? await JS.InvokeAsync<string?>("sessionStorage.getItem", "auth_token");
+
+            if (!string.IsNullOrEmpty(storedToken) && Auth.TryRestoreToken(storedToken))
+            {
+                // token restored
+            }
+            else
+            {
+                Nav.NavigateTo("/login");
+                return;
+            }
+        }
+
+        profile = await ProfileApi.GetMyProfileAsync();
+        isLoaded = true;
+    }
+
+    private sealed class OverviewSection : ComponentBase
+    {
+        [Parameter] public List<AlbumDto> Albums { get; set; } = new();
+        [Parameter] public List<TrackDto> Tracks { get; set; } = new();
+        [Parameter] public List<TourDto> Tours { get; set; } = new();
+
+        protected override void BuildRenderTree(RenderTreeBuilder builder)
+        {
+            var upcoming = Tours.Count(t => !t.IsPast);
+            var past = Tours.Count(t => t.IsPast);
+
+            builder.OpenElement(0, "div");
+            builder.AddContent(1, (MarkupString)$"<p class=\"mb-1\"><strong>Albums:</strong> {Albums.Count}</p>");
+            builder.AddContent(2, (MarkupString)$"<p class=\"mb-1\"><strong>Tracks:</strong> {Tracks.Count}</p>");
+            builder.AddContent(3, (MarkupString)$"<p class=\"mb-0\"><strong>Tours:</strong> {upcoming} upcoming, {past} past</p>");
+            builder.CloseElement();
+        }
+    }
+
+    private sealed class AlbumsSection : ComponentBase
+    {
+        [Parameter] public List<AlbumDto> Albums { get; set; } = new();
+
+        protected override void BuildRenderTree(RenderTreeBuilder builder)
+        {
+            builder.OpenElement(0, "div");
+
+            if (Albums.Count == 0)
+            {
+                builder.AddContent(1, (MarkupString)"<p class=\"text-muted\">┼áiuo metu album┼│ n─ùra.</p>");
+            }
+            else
+            {
+                builder.OpenElement(2, "div");
+                builder.AddAttribute(3, "class", "table-responsive");
+                builder.OpenElement(4, "table");
+                builder.AddAttribute(5, "class", "table align-middle");
+
+                builder.OpenElement(6, "thead");
+                builder.OpenElement(7, "tr");
+                builder.AddMarkupContent(8, "<th>Title</th><th>Year</th><th>Genre</th><th></th>");
+                builder.CloseElement(); // tr
+                builder.CloseElement(); // thead
+
+                builder.OpenElement(9, "tbody");
+                var seq = 10;
+                foreach (var a in Albums)
+                {
+                    builder.OpenElement(seq++, "tr");
+                    builder.AddContent(seq++, (MarkupString)$"<td>{a.Title}</td><td>{a.Year}</td><td>{a.Genre}</td>");
+                    builder.OpenElement(seq++, "td");
+                    if (!string.IsNullOrWhiteSpace(a.ExternalLink))
+                    {
+                        builder.OpenElement(seq++, "a");
+                        builder.AddAttribute(seq++, "href", a.ExternalLink);
+                        builder.AddAttribute(seq++, "target", "_blank");
+                        builder.AddContent(seq++, "Open");
+                        builder.CloseElement();
+                    }
+                    builder.CloseElement(); // td
+                    builder.CloseElement(); // tr
+                }
+                builder.CloseElement(); // tbody
+                builder.CloseElement(); // table
+                builder.CloseElement(); // responsive
+            }
+
+            builder.CloseElement();
+        }
+    }
+
+    private sealed class TracksSection : ComponentBase
+    {
+        [Parameter] public List<TrackDto> Tracks { get; set; } = new();
+        [Parameter] public List<AlbumDto> Albums { get; set; } = new();
+
+        protected override void BuildRenderTree(RenderTreeBuilder builder)
+        {
+            builder.OpenElement(0, "div");
+
+            if (Tracks.Count == 0)
+            {
+                builder.AddContent(1, (MarkupString)"<p class=\"text-muted\">┼áiuo metu k┼½rini┼│ n─ùra.</p>");
+            }
+            else
+            {
+                builder.OpenElement(2, "div");
+                builder.AddAttribute(3, "class", "table-responsive");
+                builder.OpenElement(4, "table");
+                builder.AddAttribute(5, "class", "table align-middle");
+
+                builder.OpenElement(6, "thead");
+                builder.OpenElement(7, "tr");
+                builder.AddMarkupContent(8, "<th>Title</th><th>Album</th><th>Genre</th><th>Language</th><th>Length</th><th></th>");
+                builder.CloseElement();
+                builder.CloseElement();
+
+                builder.OpenElement(9, "tbody");
+                var seq = 10;
+                foreach (var t in Tracks)
+                {
+                    var albumTitle = t.AlbumId.HasValue
+                        ? Albums.FirstOrDefault(a => a.Id == t.AlbumId.Value)?.Title ?? "-"
+                        : "-";
+
+                    builder.OpenElement(seq++, "tr");
+                    builder.AddContent(seq++, (MarkupString)$"<td>{t.Title}</td><td>{albumTitle}</td><td>{t.Genre}</td><td>{t.Language}</td><td>{(t.Length?.ToString(@"mm\:ss") ?? "-")}</td>");
+                    builder.OpenElement(seq++, "td");
+                    if (!string.IsNullOrWhiteSpace(t.ExternalLink))
+                    {
+                        builder.OpenElement(seq++, "a");
+                        builder.AddAttribute(seq++, "href", t.ExternalLink);
+                        builder.AddAttribute(seq++, "target", "_blank");
+                        builder.AddContent(seq++, "Open");
+                        builder.CloseElement();
+                    }
+                    builder.CloseElement(); // td
+                    builder.CloseElement(); // tr
+                }
+                builder.CloseElement(); // tbody
+                builder.CloseElement(); // table
+                builder.CloseElement(); // responsive
+            }
+
+            builder.CloseElement();
+        }
+    }
+
+    private sealed class ToursSection : ComponentBase
+    {
+        [Parameter] public List<TourDto> Tours { get; set; } = new();
+
+        protected override void BuildRenderTree(RenderTreeBuilder builder)
+        {
+            builder.OpenElement(0, "div");
+
+            if (Tours.Count == 0)
+            {
+                builder.AddContent(1, (MarkupString)"<p class=\"text-muted\">┼áiuo metu tur┼│ n─ùra.</p>");
+            }
+            else
+            {
+                var upcoming = Tours.Where(t => !t.IsPast).ToList();
+                var past = Tours.Where(t => t.IsPast).ToList();
+
+                void RenderList(IEnumerable<TourDto> list, string heading)
+                {
+                    builder.OpenElement(0, "h5");
+                    builder.AddContent(1, heading);
+                    builder.CloseElement();
+
+                    builder.OpenElement(2, "ul");
+                    builder.AddAttribute(3, "class", "list-group mb-3");
+                    var s = 4;
+                    foreach (var t in list)
+                    {
+                        builder.OpenElement(s++, "li");
+                        builder.AddAttribute(s++, "class", "list-group-item d-flex justify-content-between align-items-center");
+                        builder.AddContent(s++, $"{t.Name} ΓÇô {t.Location}");
+                        builder.OpenElement(s++, "span");
+                        builder.AddAttribute(s++, "class", "badge bg-secondary");
+                        builder.AddContent(s++, $"{t.StartDate:yyyy-MM-dd} ΓÇô {t.EndDate:yyyy-MM-dd}");
+                        builder.CloseElement();
+                        builder.CloseElement();
+                    }
+                    builder.CloseElement();
+                }
+
+                if (upcoming.Count > 0)
+                {
+                    RenderList(upcoming, "Upcoming tours");
+                }
+
+                if (past.Count > 0)
+                {
+                    RenderList(past, "Past tours");
+                }
+            }
+
+            builder.CloseElement();
+        }
+    }
+}
+
 @page "/profile"
 
 <h1>Future profile page</h1>
diff --git a/Frontend/Models/ProfileModels.cs b/Frontend/Models/ProfileModels.cs
new file mode 100644
index 0000000..ac909c2
--- /dev/null
+++ b/Frontend/Models/ProfileModels.cs
@@ -0,0 +1,53 @@
+public class UserProfileDto
+{
+    public int UserId { get; set; }
+    public string DisplayName { get; set; } = "";
+    public string Bio { get; set; } = "";
+    public string DefaultLanguage { get; set; } = "";
+    public List<string> Genres { get; set; } = new();
+    public string SpotifyUrl { get; set; } = "";
+    public string YouTubeUrl { get; set; } = "";
+}
+
+public class AlbumDto
+{
+    public int Id { get; set; }
+    public int UserId { get; set; }
+    public string Title { get; set; } = "";
+    public int Year { get; set; }
+    public string Genre { get; set; } = "";
+    public string Description { get; set; } = "";
+    public string ExternalLink { get; set; } = "";
+}
+
+public class TrackDto
+{
+    public int Id { get; set; }
+    public int UserId { get; set; }
+    public int? AlbumId { get; set; }
+    public string Title { get; set; } = "";
+    public string Genre { get; set; } = "";
+    public string Language { get; set; } = "";
+    public TimeSpan? Length { get; set; }
+    public string ExternalLink { get; set; } = "";
+}
+
+public class TourDto
+{
+    public int Id { get; set; }
+    public int UserId { get; set; }
+    public string Name { get; set; } = "";
+    public DateTime StartDate { get; set; }
+    public DateTime EndDate { get; set; }
+    public string Location { get; set; } = "";
+    public bool IsPast { get; set; }
+}
+
+public class ProfileResponseDto
+{
+    public UserProfileDto Profile { get; set; } = new();
+    public List<AlbumDto> Albums { get; set; } = new();
+    public List<TrackDto> Tracks { get; set; } = new();
+    public List<TourDto> Tours { get; set; } = new();
+}
+
diff --git a/Frontend/Program.cs b/Frontend/Program.cs
index f89187c..dbd9edd 100644
--- a/Frontend/Program.cs
+++ b/Frontend/Program.cs
@@ -18,6 +18,7 @@ builder.Services.AddHttpClient("auth", client =>
 {
     client.BaseAddress = new Uri(backendUrl);
 });
+builder.Services.AddScoped<ProfileApiService>();
 
 var app = builder.Build();
 
diff --git a/Frontend/Services/ProfileApiService.cs b/Frontend/Services/ProfileApiService.cs
new file mode 100644
index 0000000..694881d
--- /dev/null
+++ b/Frontend/Services/ProfileApiService.cs
@@ -0,0 +1,36 @@
+using System.Net.Http.Headers;
+using System.Net.Http.Json;
+
+namespace Frontend.Services;
+
+public class ProfileApiService
+{
+    private readonly IHttpClientFactory _factory;
+    private readonly AuthService _auth;
+
+    public ProfileApiService(IHttpClientFactory factory, AuthService auth)
+    {
+        _factory = factory;
+        _auth = auth;
+    }
+
+    public async Task<ProfileResponseDto?> GetMyProfileAsync()
+    {
+        var client = _factory.CreateClient("auth");
+
+        if (!string.IsNullOrEmpty(_auth.Token))
+        {
+            client.DefaultRequestHeaders.Authorization =
+                new AuthenticationHeaderValue("Bearer", _auth.Token);
+        }
+
+        var response = await client.GetAsync("api/profile/me");
+        if (!response.IsSuccessStatusCode)
+        {
+            return null;
+        }
+
+        return await response.Content.ReadFromJsonAsync<ProfileResponseDto>();
+    }
+}
+
diff --git a/backend/Controllers/ProfileController.cs b/backend/Controllers/ProfileController.cs
new file mode 100644
index 0000000..acb31d7
--- /dev/null
+++ b/backend/Controllers/ProfileController.cs
@@ -0,0 +1,47 @@
+using System.IdentityModel.Tokens.Jwt;
+using Microsoft.AspNetCore.Authorization;
+using Microsoft.AspNetCore.Mvc;
+
+[ApiController]
+[Route("api/[controller]")]
+[Authorize]
+public class ProfileController : ControllerBase
+{
+    private readonly UserProfileService _profiles;
+
+    public ProfileController(UserProfileService profiles)
+    {
+        _profiles = profiles;
+    }
+
+    [HttpGet("me")]
+    public IActionResult GetMe()
+    {
+        var userIdClaim = User.Claims.FirstOrDefault(c => c.Type == JwtRegisteredClaimNames.Sub || c.Type == "sub");
+        if (userIdClaim == null || !int.TryParse(userIdClaim.Value, out var userId))
+        {
+            return Unauthorized();
+        }
+
+        var profile = _profiles.GetProfile(userId);
+        if (profile == null)
+        {
+            return NotFound();
+        }
+
+        var albums = _profiles.GetAlbumsForUser(userId);
+        var tracks = _profiles.GetTracksForUser(userId);
+        var tours = _profiles.GetToursForUser(userId);
+
+        var dto = new
+        {
+            Profile = profile,
+            Albums = albums,
+            Tracks = tracks,
+            Tours = tours
+        };
+
+        return Ok(dto);
+    }
+}
+
diff --git a/backend/Models/UserProfileModels.cs b/backend/Models/UserProfileModels.cs
new file mode 100644
index 0000000..b893a36
--- /dev/null
+++ b/backend/Models/UserProfileModels.cs
@@ -0,0 +1,46 @@
+public class UserProfile
+{
+    public int UserId { get; set; }
+    public string DisplayName { get; set; } = "";
+    public string Bio { get; set; } = "";
+    public string DefaultLanguage { get; set; } = "";
+    public List<string> Genres { get; set; } = new();
+    public string SpotifyUrl { get; set; } = "";
+    public string YouTubeUrl { get; set; } = "";
+}
+
+public class Album
+{
+    public int Id { get; set; }
+    public int UserId { get; set; }
+    public string Title { get; set; } = "";
+    public int Year { get; set; }
+    public string Genre { get; set; } = "";
+    public string Description { get; set; } = "";
+    public string ExternalLink { get; set; } = "";
+}
+
+public class Track
+{
+    public int Id { get; set; }
+    public int UserId { get; set; }
+    public int? AlbumId { get; set; }
+    public string Title { get; set; } = "";
+    public string Genre { get; set; } = "";
+    public string Language { get; set; } = "";
+    public TimeSpan? Length { get; set; }
+    public string ExternalLink { get; set; } = "";
+}
+
+public class Tour
+{
+    public int Id { get; set; }
+    public int UserId { get; set; }
+    public string Name { get; set; } = "";
+    public DateTime StartDate { get; set; }
+    public DateTime EndDate { get; set; }
+    public string Location { get; set; } = "";
+
+    public bool IsPast => EndDate < DateTime.UtcNow.Date;
+}
+
diff --git a/backend/Program.cs b/backend/Program.cs
index 5fa1f24..177d891 100644
--- a/backend/Program.cs
+++ b/backend/Program.cs
@@ -7,6 +7,7 @@ var builder = WebApplication.CreateBuilder(args);
 builder.Services.AddOpenApi();
 builder.Services.AddSingleton<ArtistService>();
 builder.Services.AddSingleton<AuthService>();
+builder.Services.AddSingleton<UserProfileService>();
 builder.Services.AddControllers();
 
 builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
diff --git a/backend/Services/UserProfileService.cs b/backend/Services/UserProfileService.cs
new file mode 100644
index 0000000..1d08e1e
--- /dev/null
+++ b/backend/Services/UserProfileService.cs
@@ -0,0 +1,118 @@
+public class UserProfileService
+{
+    private readonly List<UserProfile> _profiles = new();
+    private readonly List<Album> _albums = new();
+    private readonly List<Track> _tracks = new();
+    private readonly List<Tour> _tours = new();
+
+    public UserProfileService()
+    {
+        // Seed demo data for a single example user with Id = 1
+        _profiles.Add(new UserProfile
+        {
+            UserId = 1,
+            DisplayName = "Demo Artist",
+            Bio = "Example musician profile used for demo purposes.",
+            DefaultLanguage = "English",
+            Genres = new List<string> { "Pop", "Rock" },
+            SpotifyUrl = "https://open.spotify.com/",
+            YouTubeUrl = "https://www.youtube.com/"
+        });
+
+        _albums.AddRange(new[]
+        {
+            new Album
+            {
+                Id = 1,
+                UserId = 1,
+                Title = "First Steps",
+                Year = 2015,
+                Genre = "Pop",
+                Description = "Debut studio album.",
+                ExternalLink = "https://example.com/first-steps"
+            },
+            new Album
+            {
+                Id = 2,
+                UserId = 1,
+                Title = "On The Road",
+                Year = 2019,
+                Genre = "Rock",
+                Description = "Live recordings from the first tour.",
+                ExternalLink = "https://example.com/on-the-road"
+            }
+        });
+
+        _tracks.AddRange(new[]
+        {
+            new Track
+            {
+                Id = 1,
+                UserId = 1,
+                AlbumId = 1,
+                Title = "Morning Lights",
+                Genre = "Pop",
+                Language = "English",
+                Length = TimeSpan.FromMinutes(3.5),
+                ExternalLink = "https://example.com/morning-lights"
+            },
+            new Track
+            {
+                Id = 2,
+                UserId = 1,
+                AlbumId = 1,
+                Title = "City Nights",
+                Genre = "Pop",
+                Language = "English",
+                Length = TimeSpan.FromMinutes(4),
+                ExternalLink = "https://example.com/city-nights"
+            },
+            new Track
+            {
+                Id = 3,
+                UserId = 1,
+                AlbumId = 2,
+                Title = "Miles Ahead",
+                Genre = "Rock",
+                Language = "English",
+                Length = TimeSpan.FromMinutes(5),
+                ExternalLink = "https://example.com/miles-ahead"
+            }
+        });
+
+        _tours.AddRange(new[]
+        {
+            new Tour
+            {
+                Id = 1,
+                UserId = 1,
+                Name = "Spring Lights Tour",
+                StartDate = DateTime.UtcNow.AddMonths(-6),
+                EndDate = DateTime.UtcNow.AddMonths(-5),
+                Location = "Baltic States"
+            },
+            new Tour
+            {
+                Id = 2,
+                UserId = 1,
+                Name = "Summer Festivals",
+                StartDate = DateTime.UtcNow.AddMonths(1),
+                EndDate = DateTime.UtcNow.AddMonths(2),
+                Location = "Europe"
+            }
+        });
+    }
+
+    public UserProfile? GetProfile(int userId) =>
+        _profiles.FirstOrDefault(p => p.UserId == userId);
+
+    public List<Album> GetAlbumsForUser(int userId) =>
+        _albums.Where(a => a.UserId == userId).ToList();
+
+    public List<Track> GetTracksForUser(int userId) =>
+        _tracks.Where(t => t.UserId == userId).ToList();
+
+    public List<Tour> GetToursForUser(int userId) =>
+        _tours.Where(t => t.UserId == userId).ToList();
+}
+
